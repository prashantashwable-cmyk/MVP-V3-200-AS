# Live End-to-End Production-Like Test (Phase 38)

Generated by `scripts/live-e2e-production-test.ts` — do not hand-edit.
Re-run: `npx tsx scripts/live-e2e-production-test.ts`.

**Date:** 2026-09-23
**Overall status: BLOCKED — MISSING CREDENTIAL.** No step in the full
company lifecycle below was executed against a live, authenticated
Firebase/Firestore backend in this sandbox — none exist here, verified
directly in `docs/production/ENVIRONMENT-READINESS.md`. Nothing in this
document is reported as PASS for a step that was not actually run live.

## The required scenario (this phase's own brief)

Lead → Customer → Site → Project → Quote → Negotiation → Contract →
Payment (sandbox) → Procurement → PO → Supplier → Production → Dispatch
→ Delivery → Installation → QC Fail → Snag → Rework → Reinspection → QC
Pass → Handover → Warranty/AMC — each step verified against live
authenticated backend infrastructure, capturing state, event, audit
event, user, timestamp, project linkage, authorization, next action,
notifications, and reconciliation.

## What IS real: the same scenario, structurally proven (not live)

`scripts/full-company-simulation.ts` (Phase 29) already runs this exact
lifecycle — 48 real assertions — through the ACTUAL legacy-screen bridges
(`src/services/legacyCommercialBridge.ts`, Phases 15-18) and the real
canonical service layer, including the full QC FAIL → Snag → Rework →
Reinspection → QC PASS loop, 2 real unauthorized-role denials, and both
Phase 09 hard gates (site-readiness, QC-pass-before-handover) proven
genuinely BLOCKING before being satisfied. This is real, executed code —
not a description of what code would do — running against the DEMO
repository (in-process, no Firestore). It proves the WORKFLOW LOGIC is
correct. **It is explicitly not proof the same steps succeed against a
live, authenticated Firestore backend with real security rules enforcing
every write** — that is exactly the gap this phase names and this
document reports honestly as blocked, not glossed over.

## Per-stage status

| Stage | What Phase 29 structurally proves | Live status |
|---|---|---|
| Lead → Customer → Site → Project | Lead captured/qualified; site survey confirms customer/site; canonical Project created | **BLOCKED** |
| Quote | Created, approved, sent via the real bridge | **BLOCKED** |
| Negotiation | `Quote.status` supports `"negotiating"`; **no legacy screen bridge exists yet** — a real, separately-documented gap, not exercised even structurally | **BLOCKED** (and structurally unproven too) |
| Contract | Auto-created by the real event bus on quote acceptance | **BLOCKED** |
| Payment | Advance payment bridged into a real, idempotent canonical Payment | **BLOCKED** |
| Procurement → PO → Supplier | PurchaseOrder created, approved, sent, accepted | **BLOCKED** |
| Production → Dispatch | Production bridged; dispatch advances Project to "delivery" | **BLOCKED** |
| Delivery | Shipment arrival, material receipt confirmed | **BLOCKED** |
| Installation | Technician check-in (Phase 09 hard gate satisfied for real), evidence captured, completed | **BLOCKED** |
| QC Fail → Snag → Rework → Reinspection → QC Pass | Real failure, auto-created Snag, rework, reinspection, real pass — the full controlled loop | **BLOCKED** |
| Handover | Final checklist, customer acceptance (Phase 09 hard gate satisfied for real), certificate issued | **BLOCKED** |
| Warranty/AMC | Auto-created by the real event bus; Project reaches its final stage | **BLOCKED** |
| Post-handover service issue | **No canonical `ServiceCase` entity/repository exists yet** — a real, separately-documented gap, not exercised even structurally | **BLOCKED** (and structurally unproven too) |

**13 stages, 0 PASS, 0 FAIL, 13 BLOCKED — MISSING CREDENTIAL.** Two of
the 13 (Negotiation, post-handover service issue) are honestly weaker
than the rest even structurally — they have no bridge/entity to exercise
at all yet, a pre-existing, previously-documented gap this phase did not
introduce or hide.

## What exactly would unblock this

The same credentials named in Phases 33/34/35: a real staging/test
Firebase project or a dedicated real-project test tenant, real per-role
test identities, and — per this phase's own explicit caution — **no real
financial transactions unless explicitly authorized and safely
configured** (a real payment-gateway sandbox/test-mode credential, not
live money movement). None of this exists in this sandbox. Running this
scenario for real, once credentials exist, means executing
`scripts/full-company-simulation.ts`'s same 28-step story through the
same real bridges, but with `ctx.environment` resolving to `'sandbox'` or
`'production'` (a real signed-in session) instead of `'demo'` — the
bridges and canonical service layer already do not need code changes to
run this way; only the credential and the willingness to write real
staging data are missing.

## Non-negotiable rule honored

Per this pack's own rule: "DO NOT use real financial transactions unless
explicitly authorized and safely configured" and "Verify every step
against live authenticated backend infrastructure... Never treat a
locally mocked identity as proof." This document does neither — it
reports the honest BLOCKED status for every live stage, and separately,
clearly labels the real (but non-live) structural proof this sandbox
already has from Phase 29.
